'use strict'
class User {
  constructor (name) {
    this.name = name
    this.rank = -8 // valid values are between -8 and 8
    this.progress = 0 // valid range 0-100
  }

  incProgress (value) {

  }
}
